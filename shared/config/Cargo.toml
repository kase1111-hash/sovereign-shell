[package]
name = "sovereign_config"
version = "0.1.0"
edition = "2021"
description = "Per-module TOML configuration library for Sovereign Shell"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
dirs = "5"
